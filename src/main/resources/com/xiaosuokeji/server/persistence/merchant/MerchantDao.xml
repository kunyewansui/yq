<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xiaosuokeji.server.dao.merchant.MerchantDao">

	<insert id="save" parameterType="merchant" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO yq_merchant(id,name,sex,mobile,business_card,debt,country)
		VALUES (#{id},#{name},#{sex},#{mobile},#{businessCard},#{debt},#{country})
	</insert>

	<delete id="remove" parameterType="merchant">
		DELETE FROM yq_merchant
		WHERE id=#{id}
	</delete>

	<update id="update" parameterType="merchant">
		UPDATE yq_merchant
		<trim prefix="SET" prefixOverrides=",">
			<if test="name!=null">
				,name=#{name}
			</if>
			<if test="sex!=null">
				,sex=#{sex}
			</if>
			<if test="mobile!=null">
				,mobile=#{mobile}
			</if>
			<if test="businessCard!=null">
				,business_card=#{businessCard}
			</if>
			<if test="debt!=null">
				,debt=#{debt}
			</if>
			<if test="country!=null">
				,country=#{country}
			</if>
			,update_time=NOW()
		</trim>
		WHERE id=#{id}
	</update>

	<select id="get" parameterType="merchant" resultType="merchant">
		SELECT *
		FROM yq_merchant
		WHERE id=#{id}
	</select>

	<sql id="listCondition">
		FROM yq_merchant
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="id!=null">
				AND id=#{id}
			</if>
			<if test="name!=null and name!=''">
				AND name=#{name}
			</if>
			<if test="dynamic!=null and dynamic.name!=null and dynamic.name!=''">
				AND name LIKE CONCAT('%',#{dynamic.name},'%')
			</if>
			<if test="sex!=null">
				AND sex=#{sex}
			</if>
			<if test="mobile!=null and mobile!=''">
				AND mobile=#{mobile}
			</if>
			<if test="dynamic!=null and dynamic.mobile!=null and dynamic.mobile!=''">
				AND mobile LIKE CONCAT('%',#{dynamic.mobile},'%')
			</if>
			<if test="businessCard!=null and businessCard!=''">
				AND business_card=#{businessCard}
			</if>
			<if test="dynamic!=null and dynamic.businessCard!=null and dynamic.businessCard!=''">
				AND business_card LIKE CONCAT('%',#{dynamic.businessCard},'%')
			</if>
			<if test="debt!=null">
				AND debt=#{debt}
			</if>
			<if test="country!=null and country!=''">
				AND country=#{country}
			</if>
			<if test="dynamic!=null and dynamic.country!=null and dynamic.country!=''">
				AND country LIKE CONCAT('%',#{dynamic.country},'%')
			</if>
		</trim>
	</sql>

	<select id="list" parameterType="merchant" resultType="merchant">
		SELECT *
		<include refid="listCondition"/>
		<include refid="XSBaseMapper.sort"/>
		<include refid="XSBaseMapper.limit"/>
	</select>

	<select id="count" parameterType="merchant" resultType="long">
		SELECT COUNT(*)
		<include refid="listCondition"/>
	</select>
</mapper>